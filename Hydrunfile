#!/bin/sh

set -e

# Binaries
if [ "$1" = "cli" ]; then
    # Install native dependencies
    apt update
    apt install -y curl

    # Install bagop
    curl -L -o /tmp/bagop "https://github.com/pojntfx/bagop/releases/latest/download/bagop.linux-$(uname -m)"
    install /tmp/bagop /usr/local/bin

    # Generate dependencies
    make depend

    # Build
    CGO_ENABLED=0 bagop -j "$(nproc)" -b ps-api -x '(android/*|ios/*)' -p 'make build-cli/ps-api DST=$DST' -d out
    CGO_ENABLED=0 bagop -j "$(nproc)" -b ps-gen-projects -x '(android/*|ios/*)' -p 'make build-cli/ps-gen-projects DST=$DST' -d out
    CGO_ENABLED=0 bagop -j "$(nproc)" -b ps-proxy -x '(android/*|ios/*)' -p 'make build-cli/ps-proxy DST=$DST' -d out

    exit 0
fi

# Frontend
if [ "$1" = "pwa" ]; then
    # Install Hugo
    apk add curl make hugo

    # Generate dependencies
    make depend

    # Build
    make build-pwa/ps-pwa

    exit 0
fi
