---
author: Felicitas Pojtinger
date: "2018-12-08"
title: Home
---

import { Base } from "../downstream/Base";
import { home } from "../data/home";
import { Header } from "../downstream/Header";
import { Links } from "../downstream/Links";
import { Skills } from "../downstream/Skills";
import { Container } from "@libresat/frontend-components";
import { Articles } from "../downstream/Articles";
import { Projects } from "../downstream/Projects";
import { graphql } from "gatsby";

export default ({
  data: {
    file: {
      childImageSharp: { fluid: image }
    }
  }
}) => (
  <Base head={home.head} background={home.background} noBackButton noContainer>
    <Container itemScope itemType="http://schema.org/Person">
      <Header {...home.content.header} image={image} />
      <Links links={home.content.links} />
      <Skills {...home.content.skills} />
    </Container>
    <Articles />
    <Projects projects={home.content.projects} />
  </Base>
);

export const query = graphql`
  {
    file(relativePath: { eq: "avatars/avatar.webp" }) {
      childImageSharp {
        fluid(maxWidth: 1000) {
          ...GatsbyImageSharpFluid_noBase64
        }
      }
    }
  }
`;

;
